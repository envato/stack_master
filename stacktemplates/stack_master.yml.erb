# prepend_region_alias_to_stack_names: true
# region_aliases:
# stack_defaults:
#   tags:
stacks:
  <%= region %>:
    <%= stack_name %>:
      template: <%= stack_name %>.json
      tags:
        environment: production
